# 实用 Python 记事工具

这个小工具会读取你输入的一句话，并把内容保存到当前目录的 `notes.txt` 文件中。

## 运行方式

确保你已安装 Python 3，然后在项目根目录执行：

```bash
python3 main.py
```

程序会提示你输入一句话：

```text
请输入一句话：
```

输入后会将内容写入 `notes.txt`，并输出：

```text
保存成功
```
